name: Deploy MultiDocker
on:
  push:
    branches:
      - main # check your repo, your default branch might be master!

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
      - run: docker build -t alfalcon/react-test -f ./client/Dockerfile.dev ./client
      - run: docker run -e CI=true alfalcon/react-test npm test

      - run: docker build -t alfalcon/multi-client ./client
      - run: docker build -t alfalcon/multi-nginx ./nginx
      - run: docker build -t alfalcon/multi-api ./api
      - run: docker build -t alfalcon/multi-worker ./worker

      - run: docker push alfalcon/multi-client
      - run: docker push alfalcon/multi-nginx
      - run: docker push alfalcon/multi-api
      - run: docker push alfalcon/multi-worker
